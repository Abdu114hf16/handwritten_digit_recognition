# handwritten_digit_recognition
An end-to-end deep learning project implementing a Convolutional Neural Network (CNN) to recognize and classify handwritten digits. The model is built using TensorFlow/Keras and features an interactive drawing canvas directly inside the Jupyter Notebook for real-time inference testing.
